#!/bin/sh
set -eu

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT_DIR"

. "$ROOT_DIR/tools/lib/crypto_backend.sh"
ai_chats_resolve_go_crypto_backend

if [ -n "$AI_CHATS_GO_TAG" ]; then
	exec go tool maubuild -tags "$AI_CHATS_GO_TAG" "$@"
fi

exec go tool maubuild "$@"
